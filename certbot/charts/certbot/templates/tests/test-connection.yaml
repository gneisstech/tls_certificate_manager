apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "certbot.fullname" . }}-test-connection"
  labels:
{{ include "certbot.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "certbot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
