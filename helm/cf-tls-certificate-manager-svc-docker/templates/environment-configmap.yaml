{{- if .Values.config.environment }}
apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app: {{ template "cf-tls-certificate-manager-svc-docker.name" . }}
    chart: {{ template "cf-tls-certificate-manager-svc-docker.chart" . }}
    component: "{{ template "cf-tls-certificate-manager-svc-docker.name" . }}"
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
  name: {{ template "cf-tls-certificate-manager-svc-docker.fullname" . }}-env
  annotations:
    checksum/environment: {{ tpl (toYaml .Values.config.environment) . | sha256sum }}
data:
{{ tpl (toYaml .Values.config.environment) . | indent 2 }}
{{- end }}
