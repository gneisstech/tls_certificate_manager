apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cf-tls-certificate-manager-svc-docker.fullname" . }}-test-connection"
  labels:
    {{- include "cf-tls-certificate-manager-svc-docker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cf-tls-certificate-manager-svc-docker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
